<!DOCTYPE html>
<html>
  <head>
    <title>🌍 Currency Manager 💸</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body class="bg-slate-900 text-emerald-600">
    <div id="app-container" class="container mx-auto">
      <nav class="py-4 px-8 bg-slate-800 max-w-full flex flex-wrap items-center justify-between rounded-b-xl drop-shadow-2xl">
        <%= link_to "the BEST Currency Manager.*", root_path, class: "font-bold text-teal-400 text-2xl" %>
        <div id="nav-items" class="grid grid-flow-col gap-8 text-slate-400">
          <% if user_signed_in? %>
            <%= link_to "Account Settings" %>
            <%= link_to "Sign Out", destroy_user_session_path, class: "hover:underline hover:underline-offset-4", 
                      data: { turbo_method: :delete, turbo_confirm: "See you soon? :)" } %>
          <% else %>
            <%= link_to "Log In", new_user_session_path, class: "hover:underline hover:underline-offset-4" %>
            <%= link_to "Register", new_user_registration_path, class: "hover:underline hover:underline-offset-4" %>
          <% end %>
        </ul>
      </nav>
      
      <!-- These p tags are used by flash notifs -->
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <div class="container py-4 px-8">
        <div class="max-w-full text-gray-400 flex flex-col gap-5 place-items-center">
          <%= yield %>
        </div>
      </div>
      <footer class="text-sm text-center mb-20 mt-8">
        <hr class="bg-slate-500 h-0.5 border-none">
        <br>
        *no guarantees<br>
        author: <%= mail_to "joao.oliveira@hole19golf.com" %>
      </footer>
    </div>
  </body>
</html>
